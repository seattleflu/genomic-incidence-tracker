import React from 'react';
// import { connect } from "react-redux";
// import { hot } from 'react-hot-loader';

const Root = () => {
  return (
    <div>
      <h1>Genomic Incidence Tracker</h1>
    </div>
  );
};

// export default hot(module)(Root);
export default Root;
